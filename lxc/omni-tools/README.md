# omni-tools

See documentation located [here][1].

```shell
   10  apt install unzip
   11  curl -fsSL https://fnm.vercel.app/install | bash
   12  source /root/.bashrc
   13  fnm
   14  fnm install 22
   15  fnm use 22
   16  fnm default 22
   17  corepack enable
   18  cd /opt
   19  git clone https://github.com/iib0011/omni-tools.git
   20  cd omni-tools/
   21  npm install
   22  apt install nginx -y
   23  ls
   24  npm run build
   25  export NODE_OPTIONS="--max-old-space-size=2048"
   26  npm run build
   27  ls
   28  npx vite build
   29  apt install nginx -y
   30  cp -r dist/* /var/www/html/
   31  systemctl restart nginx
```

[1]: <https://nicholaswilde.io/homelab/apps/omni-tools/>
